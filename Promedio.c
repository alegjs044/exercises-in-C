#include<stdio.h>

int main(){
int n,x;
float l,suma,Promedio;//n=unidades cursadas,l=limite para sacar el promedio,x=Calificacion//
x=0;

switch (extras){
	case 1: {
 
printf ("¿De cuántas unidades se quiere obtener el promedio?");
scanf("%d",&n);
}
	for(x=0;x<n;x++){
      printf("Calificacion:");
      scanf("%f",&l);
   for(;x==l+1;){
  }
      suma=suma+l;
  Promedio=suma/n;
  }
  {
	printf("El promedio es:%.2f",Promedio);
  }
}
if (Promedio<=5.9){
		Promedio=EXTRAORDINARIO;
		}
else if (Promedio=>6;Promedio<=10)
{
	Promedio=NO EXTRAORDINARIO
}

	break;
	default: printf("Opcion no valida\n");
}
	
